# Development-only dependencies (linting, testing, doc generation).
# Production deps live in requirements.txt and are NOT duplicated here.
-r requirements.txt

# ── Testing ──────────────────────────────────────────────────────────
# pytest:        Test runner
# pytest-cov:    Coverage measurement (target: ≥80% on src/)
# pytest-xdist:  Parallel test execution
pytest==8.3.3
pytest-cov==5.0.0
pytest-xdist==3.6.1

# ── Code quality ─────────────────────────────────────────────────────
# black:           Uncompromising code formatter
# isort:           Import sorting (compatible with black profile)
# flake8:          Style + common bug patterns
# flake8-bugbear:  Additional opinionated checks (B-codes)
# mypy:            Static type checking
black==24.10.0
isort==5.13.2
flake8==7.1.1
flake8-bugbear==24.10.31
mypy==1.13.0

# ── Type stubs ───────────────────────────────────────────────────────
# Needed by mypy for libraries without built-in type hints
types-PyYAML==6.0.12.20240917
pandas-stubs==2.2.3.241009

# ── Pre-commit & notebook hygiene ────────────────────────────────────
# pre-commit:  Runs hooks on git commit (lint, format, strip outputs)
# nbstripout:  Removes notebook outputs before commit (regenerable)
# nbconvert:   Programmatic notebook execution (used by `make eda` and CI)
# jupyterlab:  Interactive notebook UI for development
pre-commit==4.0.1
nbstripout==0.7.1
nbconvert==7.17.0
jupyterlab==4.3.5

# ── Report generation ────────────────────────────────────────────────
# reportlab:   Pure-Python PDF builder, used by scripts/build_technical_report.py
#              to produce reports/output/technical_report.pdf without requiring
#              an external LaTeX or Quarto installation.
# pypdf:       Page-count and text-extraction smoke tests for the generated PDF.
# python-pptx: PowerPoint builder, used by scripts/build_executive_summary.py
#              to produce reports/output/executive_summary.pptx.
reportlab==4.5.1
pypdf==5.0.1
python-pptx==1.0.2
