# ── Python build & cache ─────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.eggs/

# ── Virtual environments ─────────────────────────────────────────────
venv/
env/
.venv/
enelvenv/

# ── Jupyter ──────────────────────────────────────────────────────────
.ipynb_checkpoints/
notebooks/**/.ipynb_checkpoints/

# ── Data (immutable inputs, regenerable outputs — never tracked) ─────
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
# Tracked: the canonical engineered feature matrix that the deliverable
# notebook loads at startup. Required for evaluator-side reproducibility
# without forcing them to run the 02_feature_engineering pipeline first.
!data/processed/features.parquet
data/interim/*
!data/interim/.gitkeep
data/external/*
!data/external/.gitkeep
!data/external/README.md
# Tracked: weather data fetched from meteostat (Paris-Montsouris). Required
# by §10.4 of the deliverable notebook. Small (~660 KB) and stable.
!data/external/paris_montsouris_weather.parquet
# Small dev fixtures ARE tracked
!data/samples/

# ── Model artifacts (regenerable, versioned via registry) ────────────
models/*
!models/.gitkeep
!models/registry.json
mlruns/
.mlflow/

# ── Logs and run outputs ─────────────────────────────────────────────
*.log
logs/
outputs/

# ── Streamlit ────────────────────────────────────────────────────────
.streamlit/

# ── Testing & coverage ───────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# ── Quarto/LaTeX intermediates ───────────────────────────────────────
reports/output/*.aux
reports/output/*.log
reports/output/*.out
reports/output/*.toc
reports/source/.quarto/
_freeze/

# ── OS junk ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
